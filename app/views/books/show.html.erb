<body class="show">
	<div class="show-contents">
		<h5 class="show-title">Title: <span><%= @book.title %></span></h5>

		<h5 class="show-body">Body: <span><%= @book.body %></span></h5>
	</div>

	<h5><%=link_to "Edit", edit_book_path(@book) %> | <%=link_to "back", books_path %></h5>

	<div class="error-message">
	<% if @book.errors.any? %>
		<h2><%= pluralize(@book.errors.count, "error") %> prohibitec this article from being saved:</h2>
		<% @book.errors.full_messages.each do |message| %>
		<span><%= message %></span>
		<% end %>
	<% end %>
	</div>

</body>

